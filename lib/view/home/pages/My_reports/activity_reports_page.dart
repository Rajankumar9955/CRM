import 'package:flutter/material.dart';

class ActivityReportsPage extends StatelessWidget {
  const ActivityReportsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
          appBar: AppBar(title:Text("Activity_Reports_Page"),),
    );
  }
}