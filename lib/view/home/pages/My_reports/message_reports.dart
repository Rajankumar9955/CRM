


import 'package:flutter/material.dart';

class MessageReportsPage extends StatelessWidget {
  const MessageReportsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("MessageReportsPage"),),
    );
  }
}