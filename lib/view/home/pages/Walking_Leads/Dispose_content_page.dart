import 'package:flutter/material.dart';

class DisposeContentPage extends StatelessWidget {
  const DisposeContentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(
      title: Text("Dispose Content Page"),
    ),);
  }
}