
import 'package:flutter/material.dart';

class MyCampaign extends StatelessWidget {
  const MyCampaign({super.key});

  @override
  Widget build(BuildContext context) {
    return
    Scaffold(appBar: AppBar(title: Text("MyCampaign"),),);
  }
}